<h4><class="font-weight-normal">ジャンル編集</h4>

<%= form_with model: @genre , url: admin_genre_path(@genre)  do |f| %>

 <div class="field">
    <%= f.label :ジャンル名 %>
    <%= f.text_field :name%>
 </div>

   <%= f.submit "変更を保存する" %>

<% end %>

